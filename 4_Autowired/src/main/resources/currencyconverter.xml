<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">


	
	<bean id="currencyConverter" class="training.spring.CurrencyConverter" autowire="byName"/>
		  
	<bean id="exchangeService" class="training.spring.ExchangeService"/>
	<!-- <bean id="exchangeService2" class="training.spring.ExchangeService"/> -->
		    
		 
 			
	</beans>
	 
	 <!-- autowire="byName" - matches the id (XML) with property(pojo) ,uses setter injection-->	 
	 <!-- autowire="byType" - If Only one bean per type , uses setter injection -->
	 <!-- autowire="constructor" - Same as byType but uses constructor inject(If Only one bean per type) -->
	